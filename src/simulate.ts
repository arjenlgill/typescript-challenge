import Snap from "./snap";

function testSnapGame(): void {
  const numPlayers = 2;
  const numDecks = 1;
  const maxRounds = 10;

  const game = new Snap(numPlayers, numDecks, maxRounds);
  
}

testSnapGame();
